// The font stack is duplicated from each app's src/styles/styles.js on purpose:
// Lookahead and Labor have no styles module at all, so the package cannot import
// one. Keep this in sync with `F` in the apps that do have it.
export const F = "'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif";

export const ACCENT = '#14202a';
